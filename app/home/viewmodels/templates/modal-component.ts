import * as ko from "knockout"
import { component } from "../../../utils/decorators"

@component({
    selector : 'user-modal-popup',
    template : `<div>

    </div>`
})
class ModalPopup {
    constructor(params : any) {

    }
}
